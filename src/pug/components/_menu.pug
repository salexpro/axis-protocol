mixin menu(data)
  -
    const menu = data || [
      {
          title: 'Overview',
          link: '#link'
      },
      {
          title: 'Features',
          link: '#link'
      },
      {
          title: 'Roadmap',
          link: '#link'
      },
    ]

  ul.menu&attributes(attributes)
    each item in menu
      li
        a(href=item.link)
          = item.title
